# Build log (ARCHIVE)

> Per-feature record of what was built, what was decided and — most usefully — the
> bugs found by actually running each feature. Moved out of `CLAUDE.md` on 2026-08-19.
> Read this before changing a feature: several of these bugs are the kind that would
> be reintroduced by an innocent-looking edit.

- **✅ FEATURE 1 BUILT & SMOKE-TESTED (2026-08-18)** — folder `../cm-scheme-questionnaire/` (sibling of `Portal/`), 10 files: `index.html` (view shells) · `css/styles.css` (mockup tokens) · `js/firebase-config.js` (PASTE placeholders) · `js/firebase.js` · `js/app.js` (boot + `CONST` free-tier block) · `firestore.rules` · `service-worker.js` · `manifest.webmanifest` · `icon.svg` · `README.md` (8-step setup). **Part A concurrency = option (a) shared doc + last-edited-by banner (user approved).** Verified in-browser: setup-pending state → configured state boots to login, `window.FB` live, multi-tab offline cache active, Gujarati renders. **2 bugs found & fixed by running it:** (1) used modular-SDK name `enableMultiTabIndexedDbPersistence()` which does not exist in the **compat** SDK — correct call is `db.enablePersistence({synchronizeTabs:true})`; (2) it threw *synchronously*, aborting boot before `window.FB` was assigned, so the app died behind a misleading "Setup pending" card — `window.FB` is now published BEFORE optional persistence, and init failure shows a distinct message. ⚠️ Service-worker registration could NOT be verified (blocked in the test browser; an empty probe worker fails identically) — **verify on the real GitHub Pages deploy at Feature 9**.
- **✅ FEATURE 2 BUILT & SMOKE-TESTED (2026-08-18)** — Google sign-in + session gate. `signInWithPopup` **with redirect only as fallback** (on GitHub Pages the page and auth handler are cross-domain, so redirect breaks under third-party-cookie blocking); `prompt:'select_account'` forced (shared office machines — a reused session would misattribute answers). Session `{user, profile, isAdmin}` read **once per sign-in, held in memory** (2 reads/session, free-tier hygiene); isAdmin only draws the Admin button — rules are the real boundary. Routing: signed-out→login · signed-in+no profile→view-profile · signed-in+profile→view-home. **Rules fix:** `admins/{email}` was `allow read: if isAdmin()`, so a non-admin could not read their own doc to learn they aren't one → every officer sign-in would throw permission-denied; now `allow get` self-read + `allow list` admin-only (same for `allowlist`). **3 bugs found by running it:** (1) blocked popup → button disabled forever, no message; (2) the redirect *fallback's own* rejection was swallowed → officer pressing a dead control with zero feedback (the real dead end); (3) bad API key fell to a generic message instead of naming the fix. All fixed: 20s watchdog + fallback error reporting + bilingual setup-error messages for unauthorized-domain / operation-not-allowed / api-key-not-valid.
- **✅ FEATURE 3 BUILT & SMOKE-TESTED (2026-08-18)** — profile setup (body→name→rank→branch), written to `users/{uid}`. **RANK SEAM (user: "leave space to add rank later"):** `CONST.RANKS` keyed by body — **empty ⇒ free-text box** (officers not blocked), **filled ⇒ bilingual dropdown + "Other/અન્ય"**. Adding lists later needs NO other change (not rules, not saved profiles); a previously typed rank reopens under *Other* with text intact. Stores the **readable bilingual label, not the id** (rank appears in app bar/tracker/every audit line). Branch = Commissionerate-only, **absent not empty** on Akademis (rules-enforced); name prefills from Google displayName; profile editable via new app-bar Profile button. **Rules hardened:** new `validProfile()` (key allowlist, email pinned to token, name/rank 1–120 chars, branch required-for-Commissionerate / forbidden-for-Akademi via ternary), create/update split, `createdAt` pinned on update. **Bug found by inspecting the write payload:** edit used `set()` without merge → omitted `createdAt`, which BOTH deleted the field AND tripped the rules' `createdAt == resource.data.createdAt` check → **every profile edit would have failed permission-denied in production**. Fixed: create = plain `set`, edit = `set(merge:true)` + `FieldValue.delete()` to actively drop branch on a Commissionerate→Akademi switch. **Tested via a throwaway mock-Firebase harness** (`_test.html` + `_mock-firebase.js`, both DELETED after — do not look for them): first-login routing, all validation msgs bilingual, 3 write paths (create / edit-same-body / edit-to-Akademi), rank free-text→dropdown upgrade, and the old-free-text-rank migration case.
- **✅ FEATURE 4 BUILT & SMOKE-TESTED (2026-08-18)** — Part A question spec + render engine. **`js/spec-a.js` (1012 lines): all 25 Part A Qs, 104 sub-parts, 13 BLOCKING — verified to match the locked instrument exactly (blocking ids match; ZERO missing Gujarati anywhere — question, help, part label, every option, every write-in placeholder, table rows/cols).** Sections A1/A2/A3/A6/A7/A8/A10 (A4/A5/A9 dissolved → absent by design). **`js/render.js` (318 lines): Firebase-agnostic renderer** — types `multi` (+★ most-common marker), `single`, `text`, `longtext`, `table`; per-option write-in boxes that show only when that option is ticked; restores a saved answer set; `progress()` counts a Q answered on its FIRST part (so inapplicable follow-ups don't make a complete form read incomplete). **Will render Part B unchanged.** **ANSWER KEY FORMAT (STABLE — audit log depends on it): `A1.1.channels` · `A1.1.channels~news` (option write-in) · `A1.1.channels~~star` · `A3.2.bands~r0c1` (table cell).** **Commit model:** choices on `change`, typed text on `blur` + **no-op guard** (unchanged value writes nothing) — free-tier critical since every save also writes an audit record. **Pre-fills shown as amber "Not verified" hints, NEVER pre-written into fields** (else a guess launders itself into a verified answer). Tested via the mock harness (deleted again): 25 cards/7 section heads/13 badges/1 table/21 stars/19 prefill notes render; tick→write-in reveal; ★ single-select; table dash cell for Minister; full restore of every input type; no-op guard = 0 commits. **⚠️ Answers are IN MEMORY ONLY — nothing saves yet (that is Feature 5).**
- **✅ FEATURE 5 BUILT & SMOKE-TESTED (2026-08-18)** — Part A autosave / resume / submit to `responsesA/{bodyId}` (one shared doc per body, option (a)). **Batched autosave:** commits pool into `pending`, written after 2s quiet — verified **3 answers → 1 write** (free-tier critical; each write will also mirror to the audit log in F6). **Re-reads on EVERY open** (1 read) instead of trusting memory, flushing pending first and re-applying it over the server copy so re-read never discards unsaved work. **Offline = success not failure:** chip says "Saved on this device — will sync" (local write already stored; a spinning "Saving…" would read as broken on district connections). Failed write **puts the batch back into pending** → rides along with the next write, nothing lost. Submit sets `status/submittedBy/submittedAt`, warns-but-allows if BLOCKING Qs empty, sheet stays editable after. `beforeunload` flushes + warns. Rules: new `validResponse()` key-allowlist + `status in [draft,submitted]` + doc-id must equal `body`. **2 bugs found by running it:** (1) **shared-machine staleness — a second officer signing in on the same PC inherited the first officer's loaded sheet, so a colleague's saved answers were invisible AND the "last edited by" warning never appeared** (the entire safety net of the shared-doc model) → fixed with `resetPartA()` on user change + always-re-read; (2) banner printed a dangling "on " when the timestamp was still pending (happens in production between local save and server ack) → time now shown only once it resolves. Banner correctly stays hidden when the officer IS the last editor. **Minimal Home entry button added** (proper Home cards = F7).
- **✅ FEATURE 6 BUILT & SMOKE-TESTED (2026-08-18)** — field-level audit log (WRITE side; the admin viewer is F8). One `audit` doc per answer actually changed: `ts` · actor (`actorUid/Email/Name/Rank/Body` + `actorBranch` **Commissionerate-only, absent for Akademis**) · target (`docType` A/B, `docId`, `qid`) · `oldValue`→`newValue`. Rules = **create only, no update/delete for ANYONE incl. admins** + key-allowlist (verified: every field written is in the allowlist) + oldValue/newValue must be strings ≤400. **⭐ ATOMIC with the answer save:** response + its audit entries go in ONE `WriteBatch` — written separately, a failure could leave the log claiming a change that never saved, or an answer with no record of who made it. Verified: failed batch leaves BOTH untouched, and the retry still records the change from its ORIGINAL old value. **⭐ VALUES TRUNCATED to 300 chars (`CONST.AUDIT_VALUE_CAP`)** — log records THAT it changed, not the text (full answer lives in the response doc); untruncated, 2×5,000-char values × ~50k entries ≈ hundreds of MB against the 1 GiB free cap. Arrays flattened to "news, website". **No-op collapse:** tick+untick inside one window writes nothing; first old-value in a window is kept so a burst reads as one change. Submit writes its own `action:'submit'` entry. Cost: full Part A ≈ 200 writes vs 20,000/day free. Tested via mock harness (deleted): stamping, no-op collapse, correct old on 2nd edit, atomic failure, retry preserving original old, truncation at 301 chars, submit entry, Akademi-no-branch, separate sheets per body.
- **✅ FEATURE 7 BUILT & SMOKE-TESTED (2026-08-18)** — Home screen. Greeting (name · rank · body · branch [Commissionerate only]) + **Part A status card**: pill Not started / In progress / Submitted, progress bar, X of 25 answered, N blocking left, "last edited by" (only when it was someone ELSE), button text adapts Start / Continue / Review-or-edit. Part B card = visible placeholder (next phase) so officers see it's coming, not missing. **Read economy:** returning from the form costs **0 reads** (verified — answers already in memory and this officer just wrote them); every OTHER route to Home re-reads, since arriving from profile/admin the memory copy may be older than a colleague's save. **Failed status read shows "Status unavailable", never a confident "Not started / 0 of 25"** — the latter would make an officer think their work vanished. Tested: fresh officer → Not started/Start; after answering → In progress/Continue/4%; 0 reads returning home; colleague edit surfaces on next fresh read w/ attribution; Akademi officer shows no branch; submitted sheet → Review-or-edit; read failure path.
- **✅ FEATURE 8 BUILT & SMOKE-TESTED (2026-08-18)** — Admin tracker + audit viewer. Gated twice (button hidden + `openAdmin()` refuses); rules are the real boundary. **Tracker:** one row per body — status pill / progress bar / X of 25 / N blocking still empty / last-edited-by. **⚠️ SCOPE CORRECTION (reported to user): "Commissionerate broken down by branch" is NOT possible for Part A — Part A is ONE sheet per body, so a branch has no percentage to show. That breakdown is inherently a Part B thing (schemes are branch-tagged). Built instead: Commissionerate row lists OFFICERS REGISTERED BY BRANCH, incl. "no officer registered yet" (itself the useful signal — nobody answering for that branch). Per-scheme branch progress lands with Part B.** Officer-list failure degrades gracefully (tracker still renders all 3 bodies). **Audit viewer:** newest-first, 50/page + Load more, columns when/who(name·rank·body·branch)/question(+English text)/old→new (struck-through red → green); submit entries render as "Submitted the questionnaire". **Filters are CLIENT-SIDE over loaded entries** — server-side filter + orderBy(ts) would need a hand-made composite index in the console; the note line says so explicitly ("filters apply to loaded entries — use Load more") so an admin never wrongly concludes an entry doesn't exist. Index is the documented upgrade path. **NO live listeners anywhere** (would bill a read per officer autosave). Tested: 3-body tracker, branch officer lists + empty branches + no-branch case, 8 entries newest-first, body filter, text filter, no-match msg, 50→68 pagination, filter warning, users-read failure, audit-read failure, non-admin blocked.
- **✅ FEATURE 9 READY (2026-08-18) — 🎉 PHASE 1 COMPLETE.** Deployable tree = 15 files / 244 KB in `../cm-scheme-questionnaire/`. Added **`DEPLOY.md`** — plain-language 9-step guide written for a non-technical reader (new Firebase project · Google sign-in · Firestore **asia-south1 Mumbai**, production mode · paste config · publish rules · `admins/{email}` = user's email · GitHub repo · Pages · **⚠️ Authorized-domains step, the #1 miss**) + a **10-point post-deploy check list** + the service-worker check the user must run (F12→Application→Service Workers). Also added `.nojekyll` (GitHub Pages/Jekyll) + `.gitignore` (excludes the `_test.html`/`_mock-firebase.js` harness). **Pre-flight verified:** every file referenced by index.html exists · SW cache list matches disk exactly · nothing loaded-but-uncached · config placeholders intact · zero stray dev files · all JS parses. **Final end-to-end run (17 checks, all effectively passing):** first sign-in→profile→home→Part A→4 answer types→batched save→audit→home progress→**0 reads returning home**→submit→admin tracker+log→full re-login restore incl. table cell. (One assertion was MINE being wrong — expected 3 audit entries, got 4 correct ones: tick, write-in box, ★ marker and table cell each log separately.) **⚠️ STILL UNVERIFIABLE LOCALLY: service-worker registration** — blocked in the dev browser (an empty probe worker fails identically); must be checked on the live site. Failure is caught and harmless (app runs online-only).
- **🚧 PHASE 2 STARTED (2026-08-19). ✅ FEATURE 10 DONE — scheme seed data for ALL 3 BODIES (`js/schemes.js`, 104 schemes).** ⚠️ **The scheme-list gap is CLOSED** — user supplied 2 new files in `Portal/Schemes/` on 2026-08-19:
- **✅ FEATURE 11 BUILT & SMOKE-TESTED (2026-08-19)** — Part B scheme picker on Home. Searchable list (162 schemes is far past dropdown scale). **Commissionerate → branch selector DEFAULTING to the officer's own branch** ("All branches" one click away, not a default wall of 83); **Akademi → no selector**. Search matches EN name, GU name, id, budget head and `patrak N` (verified: `યોગ`→8 yoga schemes, `navratri`→P5-03, `patrak 9`→8). **14 admin/establishment lines hidden** with the hidden-count shown so figures still reconcile vs the GR. **Ordering: Akademi's OWN numbered list first, then GR Patrak lines** (S-01…S-51, then P8/P10) — that's the numbering its officers recognise; Commissionerate = plain Patrak order. Patrak-5 rows render branch as `Celebration (to confirm)` so the 40 guesses stay visible. **⭐ READ COST: ONE query `responsesB where body==<bodyId>`, NOT one read per scheme** — a response doc only exists once someone has answered, so the whole picker cost **3 reads** in test and never costs 162. Status pills (In progress / Submitted) come from that same query. Clicking a scheme opens `view-partb` (stub — questions are F12). Applicant-facing: Commissionerate 83 (culture 22 · celebration 17 · adventure 11 · youthBoard 33) · Lalit Kala 11 · Sangeet Natak 54.
- **✅ FEATURE 12 BUILT & SMOKE-TESTED (2026-08-19)** — officers can report a scheme filed under the **wrong body or branch** (user request; 43 branch tags are guesses and body can be wrong too). **⭐ KEY DESIGN: a report does NOT move the scheme** — it lands in `schemeFixes` for an admin to apply. Rationale: if an officer reassigned directly and got it wrong, the scheme would vanish from their OWN list and they'd have no way to find it again to undo it. Screen says this in both languages so nobody expects an instant change. **Applied corrections = Firestore `schemes/{schemeId}` overrides `{body?, branch?, branchConfirmed}` layered over `js/schemes.js` at load → fixes need NO redeploy**; applying also clears that scheme's "(to confirm)" mark. **Write ORDER: override first, THEN mark the report handled** — reversed, a failure would leave a report marked done with the scheme never moved, the one outcome nobody notices. Rules: `schemes` write = admin-only w/ key allowlist + body/branch enum; `schemeFixes` create = signed-in w/ actor pinned to token, `get` own-only, `list` admin-only, **update restricted to status/resolvedBy/resolvedAt so the officer's original wording can never be rewritten**, no delete. Admin screen gained a **pending-corrections panel** (Apply / Dismiss). **Bug found by running it:** search box + branch selector persisted across a user change on a shared machine → officer B saw "Showing 0 of 85" and would reasonably think their schemes had vanished; fixed by resetting picker state on uid change (same class as the F5 Part A staleness bug). Verified end-to-end: report → admin sees it → Apply → scheme actually moves bodies, disappears from the old list, appears in the new one, guess-mark cleared.
- **✅ FEATURE 13 BUILT & SMOKE-TESTED (2026-08-19)** — Part B question spec. **`js/spec-b.js` (1881 lines): all 58 Part B questions, 187 sub-parts, 528 options, 142 write-in boxes, 39 BLOCKING — verified against the locked instrument** (ids and order match exactly; blocking flags match one for one; ZERO missing Gujarati anywhere — question, help, part label, every option, every write-in placeholder, every table row and column). Sections B0/B1/B2/B3/B4/B5/B7/B8/B9/B10/B11. **Deliberate absences, each with its reason in the instrument:** B6 dissolved into Part A (A10 rules, A8.4 "caught double funding") so the number is missing by design, as A4/A5/A9 are in Part A; plus B4.5 (redundant with A1 + B4.4), B5.4 (troupe size is art-form-level, not scheme-level), B7.7 (rejection letters are universal → A3.5), B8.5 (Aadhaar covered by B5), B9.7 (folded into B2.8). **`render.js` NOT touched — Part B renders on the existing engine exactly as predicted at Feature 4**, and the answer-key format is unchanged (`B0.5.mechanism` · `B4.3.docs~aadhaar` · `B2.1.whoApplies~~star` · `B1.1.orders~r0c0`), so the audit log keeps working with no change. **3 PRESENTATION DECISIONS (instrument unchanged, only how it is drawn):** (1) **B8.6's 8×5 timing grid = eight choose-one rows** (`t1`…`t8`) — the engine's `table` type makes typing boxes, and "mark the usual band" must be a choice, not free text; (2) **the M/O marker (B4.3, B9.1) and A/P marker (B5.1) = the per-option write-in box**, which is exactly what the paper form does (tick the document, write M or O beside it) and it stays hidden until its option is ticked; (3) **B3.5's "no rule" ticks are a separate list above the min/max grid** — a blank grid cell cannot distinguish "there is no rule" from "not answered yet", and that distinction is the point of the question. Only two tables survive: B1.1 (orders, 6×3) and B3.5 (event size, 4×2). **Tested via the throwaway harness (`_test.html`, deleted after — `.gitignore`d), 39 assertions, all passing:** whole of Part B renders through the real engine with no throw and no "unsupported type" fallback · 58 cards / 11 section heads / 39 blocking badges / 2 tables / 753 inputs · B0.5 draws 11 tick-boxes and zero radios (it is tick-all — getting this wrong would silently destroy the router) · all 19 M/O boxes in B4.3 start hidden and reveal on tick · B8.6's eight rows each offer the 5 bands and hold their answers independently of one another · a saved sheet restores exactly for every input type incl. table cell, ★ marker and a revealed write-in · no-op guard = 0 commits on untouched blur, 1 commit after a real edit · progress() = 0 of 58 with 39 blocking on an empty sheet, and a ticked-then-unticked list correctly does NOT count as answered · answer keys come out in the stable format. **No bug found in the spec this time** — the one thing worth recording is what the harness was written to catch: B0.5 rendering as choose-one instead of tick-all, which would have let an officer record only one mechanism for a scheme that does several, and quietly mis-route the whole per-scheme questionnaire in Feature 14. **⚠️ Nothing saves yet — Part B answers are in memory only (that is Feature 15).** Wired into `index.html` and the service-worker shell list; `CACHE` bumped v3 → v4.
- **✅ FEATURE 14 BUILT & SMOKE-TESTED (2026-08-19)** — the Part B form on screen, plus **"ask only what applies"**. New **`js/route-b.js`** holds **exactly six routing rules**, and the file's own header states the constraint they were built under: **wrongly setting a question aside is invisible** (the fact is never captured and nobody learns it was missed) while **wrongly asking one costs three seconds** — so every rule DEFAULTS TO SHOWING and fires only on a clear positive answer. An unanswered driver question therefore sets nothing aside. **Five of the six rules are transcribed from the locked instrument, not invented** — B2.6 + B5.3 ("skip if only individuals apply", from B2.1), B7.5 ("only if a committee exists", from B7.4), B10.2 + B10.3 ("answer only if run at district level", from B10.1). **The sixth is the one inference:** B0.5 mechanisms `engaged`/`provided` mean the office runs its own event, so nobody applies and the five B4 application questions fold — and it is deliberately narrow, firing only when EVERY ticked mechanism is one of those two (add `grant`, or even `combination`, and B4 returns immediately). **⭐ TWO DELIBERATE NON-RULES, both recorded in the file:** (1) **B9 is NOT routed away for award/pension schemes** — B9.1 and B9.2 already carry "No — paid without any further documents (e.g. award / pension)" and "Not applicable (award / pension)" as ANSWERS, so folding the block would throw away the very fact worth recording; (2) **B5.2 is left alone** — its condition is the art form (culture vs youth/sports/yoga/adventure), which the app holds nowhere. **⭐ SET ASIDE IS A FOLD, NEVER A DELETION.** A folded question shows what was set aside, WHY (naming the driver question, bilingual), how many answers are already inside it, and a **Show anyway** button; a whole folded section collapses to ONE card rather than a run of identical rows. **Answers inside a fold are never erased** — correcting B0.5 after filling in B4 keeps all of it, and the fold says "3 answer(s) already recorded here are kept, not deleted" in both languages, because folding over existing work is the one case that could read as data loss. **Officer override:** any section except B0 and B11 can be marked "This section does not apply" by hand — their judgement about their own scheme beats our rules — and the fold then says it was their decision. **B0 is excluded on purpose: it holds B0.5, which every rule reads, so the router can never be hidden from the officer.** A **"Show every question"** button opens every fold at once, the escape hatch for our rules being wrong. **Counting is routed, not flat:** the denominator is what this scheme is actually asked ("12 of 45 answered", plus "5 of the 58 questions are set aside"), because against a flat 58 a routed scheme could never read as complete and every scheme would sit permanently short on the tracker. **Redraw is deliberately narrow** — only the four driver keys (`B0.5.mechanism`, `B2.1.whoApplies`, `B7.4.involvement`, `B10.1.decidedWhere`) reshape the form; any other answer just repaints the counters, because redrawing on a committed text answer would throw the officer out of the box they are typing in. Redraw preserves scroll position. **Bug found & fixed (pre-existing, from F11):** the scheme picker computed Part B progress against **`window.SPEC_A`** — a leftover from before `spec-b.js` existed — so the moment saving lands in F15 every scheme row would have shown a wrong figure; now counted against SPEC_B **and** against the routed denominator. **Bug found by running it (mine, mobile):** the new "This section does not apply" control sat to the right of a bilingual section heading and, at 375px, was pushed past the screen edge — district officers work on phones. Fixed by stacking `.sec-head-row` and `.fold-row` under 600px; verified zero overflowing elements at 375px and 1280px. **Tested with two throwaway harnesses (both deleted, `.gitignore`d).** `_test.html`, 44 assertions on the rules in isolation: nothing set aside before any answer · each rule firing and, more importantly, each rule NOT firing ("mixed", "don't know", "other", an extra applicant type, an emptied tick-list all keep the question) · all six stacking (48 asked, 10 set aside, blocking 39 → 31) · folding never deletes and the kept-answer count is right · every fold can be opened, singly or by section · officer override incl. B0/B11 being refused · only four watched keys · the rules touch only the 10 agreed questions and never B5.2. `_app.html` + `_appcheck.js` + `_mock-firebase.js`, 41 assertions driving **the real app.js end to end**: sign in → pick a scheme → 58 cards, 11 section heads, "0 of 58 · 39 blocking" → tick B0.5 → B4 folds into one card, counter follows to "of 53 · 33 blocking" → Show anyway restores it → answer B2.6 then say individuals-only, and the fold reports the answer kept → officer marks B9 N/A and restores it → **typing an ordinary answer does NOT redraw** (verified the very input element survives) → "Show every question" clears every fold → **opening a second scheme starts clean, with no answers, no ticks and no folds carried over from the first** (the shared-machine failure class that has now bitten this project three times). **Two harness assertions were MINE being wrong, not the app's** — 8 of the 10 routed questions are blocking (not 9), and B0.5 counts as an answer, so the expected figures were 33 and 2, which is what the app produced. **⚠️ NOTHING SAVES YET — Part B answers are in memory only until Feature 15, and an amber banner on the screen says so in both languages.** Officer-marked N/A sections are likewise memory-only and need a home in the response document in F15. `CACHE` bumped v4 → v5.
- **✅ FEATURE 15 BUILT & SMOKE-TESTED (2026-08-19)** — Part B autosave / resume / submit into `responsesB/{schemeId}`, one document per scheme. Deliberately the **same machinery as Part A**, not a second implementation: 2s commit-debounce, no-op guard, **response + its audit entries in ONE atomic `WriteBatch`**, failed write puts the batch back into `pending`, offline reported as "Saved on this device — will sync". Verified 3 answers → **1 write** (4 docs in the batch: the sheet + 3 audit rows). **Approved concurrency = anyone in the SAME OFFICE** (user's decision) — deliberately NOT narrowed to the scheme's branch, because 43 branch tags are still unconfirmed guesses and a wrong tag would lock the right officer out of their own scheme with no way back. **Rules now enforce it server-side:** new `myBody()` reads the caller's own profile and `isMyBody()` requires the sheet's body to match, so an officer of one Akademi can no longer write another body's sheets; also `request.resource.data.scheme == schemeId`, without which a client could file one scheme's answers under another's id. Costs one rules `get()` per response write — negligible against 50,000 free reads/day, and it is the difference between a real boundary and a suggestion. **⭐ BUG FOUND WHILE WRITING IT (would have corrupted data silently):** leaving a scheme flushes it under its own id *after* `partB` has been pointed at the next scheme, so a **failed** write would have put the OLD scheme's answers back into `pending` and the next save would have filed them **under the NEW scheme** — one scheme's answers landing in another's sheet, which nobody would ever spot. Both the `then` and `catch` handlers now check `partB.schemeId === id` first; if the officer has moved on, the change is dropped loudly (console + no log entry) rather than written to the wrong place. Harness covers exactly this: type, leave inside the 2s window, open another scheme — the answer lands in the first scheme and is absent from the second. **⭐ BUG FOUND BY RUNNING IT:** after saving a scheme and pressing Save & exit, the scheme list still showed it as untouched — Home does not re-read when returning from a form (the "0 reads returning home" rule from F7), so `picker.statuses` was stale, and the natural reading of that is *"my work did not save"*. Fixed by `rememberPartBStatus()` updating that scheme's row **from memory, at zero read cost** — we just wrote the data, so we know it. **Officer-marked N/A sections are now saved**, as `naSections: [{id, by}]` — a **list, not a map, on purpose**: a merge write merges nested maps rather than replacing them, so a map would have kept a mark the officer had just cleared. Verified both directions (mark → saved with who decided it; un-mark → actually gone). The fold now names the colleague who set a section aside, so "you marked this section" is never shown to someone who did not. **Also carried in the sheet:** `body`, `scheme`, and the scheme's `branch` (so F17's per-branch tracker can group without re-deriving it). **Audit viewer taught Part B** — `questionText()` now falls back to `SPEC_B`, and resolves the whole-section marks, otherwise every per-scheme change would have read as a bare id with no text. **`chip()` refactored to `setChip(id, …)`** so both forms share one set of wordings; Part A's wrappers kept their old signatures and Part A saving was re-tested as a regression. **Tested with a throwaway harness (deleted, `.gitignore`d) — 47 assertions, all passing:** batching · atomic answer+audit · what actually lands in the document · no-op guard · N/A saved and un-saved · leaving and resuming restores every input type · **one scheme's work never reaching another** · forced save failure → officer told, nothing on the server, retry rides along with the next change and keeps the original old value in the log · submit warns on empty blocking questions, stamps who and when, and an edit afterwards does not silently drop the sheet back to draft · the scheme list shows In progress / Submitted / "N of M answered" · **Part A still saves** after the chip refactor. **⚠️ Remaining gap, unchanged and now the only one: NO FILE UPLOADS.** Several questions ask the officer to attach a blank form, a GR copy or a UC format; the free tier has no Cloud Storage, so those stay as typed answers. Flagged to the user as a separate decision. `CACHE` bumped v5 → v6.
- **✅ FEATURE 16 BUILT & SMOKE-TESTED (2026-08-19)** — confirming the **43 guessed branch tags** (26 guessed Culture, 17 Celebration; 40 from Patrak-5 plus P8-01/P10-01/P10-02). **⭐ APPROVED SPLIT (user chose "branch officer"): officers CONFIRM, only an admin MOVES.** Confirming leaves a scheme exactly where it is, which is what makes it safe in an officer's hands; moving it would make it vanish from their own list with no way to find it again — the exact trap Feature 12 was built to avoid — so reassignment still goes through a report an admin applies. **Officer half:** on the scheme's own screen, next to the existing "Report a correction", a **"Yes — confirm this branch"** button that appears ONLY when the branch is still a guess AND it is the officer's own branch AND their own body — they are the person who would know. A bilingual note says what confirming does and points to the report route if it is wrong. **Admin half:** a new panel listing every unconfirmed tag with name (both languages), Patrak, allocation and the branch we guessed, plus **Confirm** and a **Change to…** picker. **Deliberately NO "confirm all"** — one click clearing 43 guesses would destroy the entire point of asking; the on-screen note says so. **⭐ THE EVIDENCE LINE:** each row shows *which branch is actually answering that scheme's questionnaire* — if the Celebration officer is filling it in, it is almost certainly a Celebration scheme, and the row says plainly when that DISAGREES with our guess. This is why F15 stores `lastEditedByBranch` (the editor's own branch, not the scheme's); costs **one query** for the whole panel, never one read per scheme, and it is useless on day one but better every week. **⭐ BUG-CLASS FOUND BY RUNNING IT:** 3 of the 43 (`P5-31` corpus fund, `P5-32` theatre establishment, `P5-36` publicity budget) are **establishment lines hidden from officers**, so no officer can ever confirm them and no evidence will ever appear for them — an admin could have waited indefinitely for input that cannot come. Those rows are now labelled "Establishment line — officers never see this one, so only you can settle it". **Rules:** `schemes/{id}` gains an officer path — `hasOnly(['branchConfirmed','updatedBy','updatedByEmail','updatedAt'])` on create and, on update, `diff(resource.data).affectedKeys().hasOnly([...])`, so **body and branch can never be touched by an officer even though a merge write carries them along**; `branchConfirmed` must be `true`; email pinned to the token. Admin keeps the full path. **⚠️ HONEST LIMIT recorded in the rules:** they cannot verify the scheme really is in the officer's branch — a scheme's branch lives in `js/schemes.js`, not Firestore, so there is nothing server-side to compare against. The client only offers the button for the officer's own branch; the worst case server-side is an officer confirming a tag that is not theirs, which **moves nothing, hides nothing, is stamped with their email, and an admin can overrule**. **Audit:** new `docType 'S'` (the scheme's own record) so branch changes are attributable; the audit viewer resolves `qid: 'branch'` to "Which branch runs this scheme", otherwise the row would read as a bare id. **The audit entry is written separately and second, on purpose** — a scheme write and an audit write pass different rules, and a rejected log row must not roll back a legitimate tag change (a tag change with no log line is recoverable; a tag that silently refused to save is not). **Tested with a throwaway harness (deleted, `.gitignore`d) — 37 assertions, all passing:** 43 listed and counted (`0 of 43 confirmed` → moves as they go) · Confirm writes `branchConfirmed` and **does NOT rewrite the branch** · Change actually moves it and logs from→to · a confirmed scheme **stays in the officer's list** with its "(to confirm)" mark gone · the officer's button appears for their own branch and **an officer writes ONLY the confirmation, never branch or body** · **a Culture officer is NOT offered confirm on a Celebration scheme** · a refused write leaves the row in place and re-enables its buttons rather than silently clearing it · the evidence line appears and names the disagreement · the 3 establishment lines are labelled. **Two harness assertions were MINE being wrong:** I checked row[0] for the "(to confirm)" mark (the list is Patrak-ordered, so row[0] is often already confirmed — 37 rows do carry it), and I searched the whole admin screen for "confirm all", which matches **my own note explaining why there is no such button**. Mobile checked at 375px: no overflow in the new panel. `CACHE` bumped v6 → v7.
- **✅ FEATURE 17 BUILT & SMOKE-TESTED (2026-08-19) — 🎉 PHASE 2 COMPLETE.** Part B progress **branch by branch** — the breakdown that was impossible for Part A (one shared sheet per body, so a branch has no percentage of its own) and the whole reason Part B is per scheme. **Six groups: Culture 25 · Celebration 17 · Adventure 11 · Youth Board 33 · Lalit Kala 11 · Sangeet Natak 51 = 148**, verified to reconcile against the applicant-facing total. **The 14 establishment lines are excluded** — nobody applies for an advertising budget, and counting them would leave every branch permanently unfinishable. Each row: schemes / submitted / in progress / not started, a bar, "N of M questions answered across the sheets already started", and the blocking count. **⭐ "Important questions still blank" counts ONLY schemes somebody has started** — dragging in the untouched ones would bury the real signal under 39 × every scheme nobody has opened, and "not started" is already its own column. **⭐ The untouched schemes are NAMED**, behind a fold ("Show the 11 nobody has started") — "33 not started" gives an admin nothing to act on; which 33 does. Folded by default or Youth Board's list alone would dominate the screen. **Officers see their own branch on Home** (user's decision): "Your branch — Culture · 25 schemes · 1 submitted · 2 in progress · 22 not started", with a bar and a status pill. **Costs nothing** — it reuses the query the scheme picker already makes, and is recomputed from memory when the officer returns from a scheme, so it is right before any read comes back. **Copy summary** (user's decision) puts the whole table on the clipboard as plain text with a TOTAL line, ready to paste into a note upward; **⭐ a refused clipboard falls back to showing the text selected in a box** — plain http and locked-down district machines both deny `navigator.clipboard`, and a button that silently does nothing is worse than no button. **⭐ TWO BUGS FOUND BY RUNNING IT:** (1) **the officer's own-branch line never appeared on a fresh sign-in** — it was painted from `initSchemePicker()`, but the scheme statuses arrive one query later, so `picker.loaded` was still false at that moment and the box stayed hidden until something else redrew the screen; now painted wherever the list is painted (`renderSchemeList()` wraps `renderSchemeListRows()`), covering both the sync and async paths. (2) **the admin screen queried `responsesB` for the Commissionerate TWICE** — once for this tracker and once for Feature 16's evidence line; now `loadAdminPartB()` loads once and renders both panels, so the whole admin screen costs **3 responsesB queries, one per office, never one per scheme**, and the two panels can no longer drift apart on screen. **A failed read says so** — "Could not read N of 3 offices — the figures below are incomplete", never a confident "0 submitted", which is the one wrong number that would send someone chasing work already done. **Tested with a throwaway harness (deleted, `.gitignore`d), seeded with 3 Part B sheets — 35 assertions, all passing:** the officer's line shows their branch and ONLY their branch · one query on Home · six groups totalling 148 · establishment lines excluded · per-branch splits correct · a branch with nothing started says so rather than showing a blank bar · blocking counted only across started sheets · the untouched list folds, opens, names all 11 and closes · copy summary contains every group and the TOTAL · **the clipboard-refused fallback shows the same text** · a failed read reports incomplete figures · Refresh recovers · the officer's line moves after they start a scheme and is already correct before any query returns. **One harness failure was MINE:** I asserted zero reads on returning Home, but Home makes its own refresh read whenever Part A is not in memory (Feature 7's rule) — Feature 17 adds none on top of it, which is what the assertion now says. Mobile checked at 375px: no overflow. `CACHE` bumped v7 → v8.
- **✅ FIX (2026-08-19) — Sign out was unreachable on a phone.** The app bar was a single non-wrapping flex row (brand left, officer name + up to four buttons right); at 375px the last button, **Sign out**, was pushed past the screen edge with no way to reach it. **This matters more than it looks: these are shared office machines, and an officer who cannot sign out leaves the next person signed in as them** — the same failure class as the stale-state bugs in F5, F11 and F15. Fixed in the `max-width:600px` block only: the bar wraps, the officer's name takes its own line and is trimmed with an ellipsis rather than shoving the buttons along, and the buttons wrap under it. **Second-order problem caught by measuring rather than eyeballing:** wrapped, the bar needs three rows ≈ **180px, a fifth of an 812px phone screen** — and being `position:sticky` it would have sat on top of a 58-question form the whole way down, trading one bug for a worse one. **The bar is therefore `position:static` under 600px**: it scrolls away, and nothing depends on it mid-form because every form carries its own Save & exit at the bottom. Verified at 375px (Sign out fully on screen at 20–165, zero overflowing elements anywhere, bar scrolls off) and at 1280px (**unchanged** — still sticky, still one row, Sign out at 1034–1133). `CACHE` bumped v8 → v9.
- **✅ DEMO MODE + LOCAL RUN (2026-08-19)** — the user asked to try the app before deploying, which was fair: nothing could be seen without a Firebase project. **`js/demo.js` — OFF unless the address ends in `?demo=1`.** Without the flag the file returns on its first line and the real Firebase path is completely untouched (verified: plain `localhost:8000` still shows "Setup pending", no demo bar, `window.FB.demo` undefined). With it, `window.FB` is replaced wholesale — fake signed-in officer, in-memory database with Firestore-accurate merge semantics, a pre-made Culture-branch AD profile, admin rights, and seeded work (part of Part A, one submitted scheme, one draft, a colleague as last editor) so the progress screens are not empty. Loaded **after `firebase.js` and before `app.js`**, so it needs no change to either. **The risk this file exists to manage:** someone handed a demo link could fill in a whole scheme and lose it — so there is a red bar fixed to the bottom, bilingual, that cannot be dismissed. **⭐ THREE BUGS IN THE BAR, ALL FOUND BY MEASURING RATHER THAN LOOKING:** (1) a hard-coded `padding-bottom:76px` was not enough at 375px, where the warning wraps to **111px** — the bar sat on top of the last card; now the page reserves the bar's **measured** height. (2) Measuring only inside `requestAnimationFrame` left the padding at **zero in a hidden/background tab**, because rAF does not run there — a demo link opened in a background tab would have had no spacing at all; it now applies immediately **and** again next frame. (3) Measuring immediately on `resize` kept the **old** height when a narrow window was widened, leaving a large empty gap — hence the second, deferred pass. Verified at 375px (bar 111px, page bottom clear, Part B's Submit button reachable) and 1280px (bar 56px, padding 68px, footer clear). **Full click-through in demo mode passed:** Home → open a scheme → 58 questions → B0.5 = "engaged" folds B4 to 53 → type an answer → "Saved ✓" → Save & exit → list shows "2 of 53 answered · In progress" → branch line updates → reopen → answer restored → Admin shows six branch rows totalling 148, 43 tags to confirm, and audit entries. **New `RUN-LOCALLY.md`** — plain-language, two ways: demo mode (`python -m http.server 8000` then `?demo=1`, no setup at all) and the real app locally against Firebase (steps 1–6 of DEPLOY.md only; **Google sign-in allows `localhost` by default, so the Authorized-domains step is not needed until the site is on a real address**). Both `python` and `py` verified present on this machine. `DEPLOY.md` also brought up to date — its check list covered only Part A and predated Features 10–17; it now has 10 Part B checks, the correct cache-version number, per-scheme write costs, a "republish the rules if saving fails" note, and wording to send officers. `CACHE` bumped v9 → v10.
- **🚀 DEPLOYED & LIVE (2026-08-20).** Live at **https://a-gyani.github.io/ks-scheme-questionnaire/** · repo **https://github.com/A-gyani/ks-scheme-questionnaire** (public) · Firebase project **`kalasetuquestion`**, Firestore in **asia-south1 (Mumbai)**, **Spark (free) plan**. Deployed step by step with the user doing everything that touches their own accounts and Claude doing everything else. **⭐ THE ONE UNKNOWN SINCE FEATURE 1 IS RESOLVED: the service worker registers and activates on the live site** (`activated`, cache `csq-shell-v11`) — it could never be verified in any of F1–F17 because the dev browser blocks it. Offline use and installing the app both work. **Live verification (Claude, unauthenticated):** every file serves over HTTPS, **zero console errors**, boots to the sign-in screen (not "Setup pending"), connected to the right project, all content present (25 Part A · 58 Part B · 162 schemes · 6 routing rules). **Full flow exercised on the deployed code via `?demo=1`:** scheme opens with 58 questions → B0.5 "engaged" folds it to 53 with the correct "5 set aside" line → answer saves ("Saved ✓") → returning Home shows "2 of 53 answered · In progress" → admin branch table totals **148 schemes across 6 rows**, 43 tags listed, audit entries recorded. **Mobile at 375px: nothing overflows and Sign out is reachable**, so the app-bar fix holds up live. **Verified by the user (signed in, which Claude cannot do):** lands straight on Home rather than the profile screen, the Part A answers made on `localhost` are present on the live site — proving both point at the same database — and the audit log shows their changes under their own name. **⚠️ TWO THINGS THAT BIT DURING DEPLOYMENT, both worth remembering.** (1) **"Setup pending" on localhost after the config was pasted in** — the service worker was serving the *cached* `firebase-config.js` from demo-mode testing, still carrying the `PASTE_…` placeholders. Fixed by bumping `CACHE` and clearing site data. This is exactly the failure `DEPLOY.md` warns about, and it will recur for officers on every future change unless the cache version is bumped. (2) **`DEPLOY.md` was wrong about private repositories** — GitHub Pages serves from a private repo only on a paid plan, so a free account needs a **public** repo; corrected, and the personal email address was removed from `DEPLOY.md`/`README.md` since they are now public. **⚠️ ACCESS IS DELIBERATELY OPEN (user's decision, asked and answered):** `isAllowed()` is still `isSignedIn()`, so anyone with any Google account who finds the URL can sign in and answer. Every change is stamped with their name and email and is auditable. The allowlist gate is written and commented out in `firestore.rules`; switching it on is a rules edit plus one `allowlist/{email}` doc per officer, and needs no code change.

